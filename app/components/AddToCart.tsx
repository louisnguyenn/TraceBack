'use client'; // tells the next.js complier to run in client side
import React from 'react';

const AddToCart = () => {
	return (
		<div>
			<button onClick={() => console.log('Click')}></button>
		</div>
	);
};

export default AddToCart;
